# Оценка готовности проекта к сдаче

**Дата:** 2026-06-22  
**Состояние репозитория:** `4a1bfa0` — код презентации + тесты метрик/ансамбля  
**Kaggle:** новые сабмиты **невозможны**; зафиксирован лучший командный скор **349.31** (ранг ~90)

---

## 1. Соответствие ТЗ (`Разработка/подготовка/ТЗ_Хакатон.txt`)

| Требование ТЗ | Статус | Где в репо |
|---------------|--------|------------|
| Предсказание IC50, CC50, SI | ✅ | `train.py`, `predict.py`, `TARGETS` |
| Pipeline решения | ✅ | `run_pipeline.py --train` / `--predict` |
| CSV: `index, IC50, CC50, SI` | ✅ | `predict.py`, `postprocess.py` |
| Метрика: mean(RMSE×3) | ✅ | `utils/metrics.py` → `competition_score` |
| Код в Git | ✅ | `ml/src/chemai/` |
| README + воспроизведение | ✅ | `README.md`, `.env.example`, `Makefile` |
| Презентация | ✅ | `docs/presentation/39_Презентация_ChemAI-Predict-the-Cure.pdf` |
| Видео (топ-10) | N/A | Команда не в топ-10 |

**Замечания (некритичные):**

- Данные `train.csv`/`test.csv` **не в git** (`.gitignore`) — допустимо при инструкции в README.
- SI прогнозируется **отдельной моделью**, не через IC50/CC50 — соответствует формулировке ТЗ («как отдельную переменную»).

---

## 2. Презентация vs код (актуальность)

| Утверждение презентации | Код `4a1bfa0` | Совпадение |
|-------------------------|---------------|------------|
| ClusterKFold | `validation/cv_splitter.py` → `ClusterKFold` | ✅ |
| Per-fold Preprocessor | `train.py` — fit только на train фолда | ✅ |
| 9 семейств + опц. CatBoost | `build_default_candidates` | ✅ |
| Взвешенный ансамбль, **без stacking** в `train.py` | `Ensemble`, веса из CV RMSE | ✅ |
| log1p IC50/CC50 + expm1 | `Expm1Predictor`, config flag | ✅ |
| Лучший LB **~349** (submission2_a4) | Вне `train.py`; CSV в `docs/submissions/` | ✅ документировано |
| Baseline repo ~365 | Типичный `final_submission.csv` | ✅ |
| tools/ удалён | Нет каталога `tools/` | ✅ |

### Требуется ли обновление презентации?

**Нет — для сдачи.** PDF и драфт согласованы с текущим кодом.

**Опционально** (не блокирует сдачу, если не заявлять как «уже в git»):

- Упомянуть **345.08** как офлайн-эксперимент 22.06 (Epic 10, откатан) — только в записке/бэклоге, не в слайдах.
- Добавить слайд «9 тестов pytest» — усилит критерий «качество кода».

---

## 3. Соответствие обученным моделям

| Артефакт | Ожидание | Факт |
|----------|----------|------|
| `pipeline_bundle.joblib` | После `--train` на полном train | Локальный; **не в git** |
| Лучший Kaggle | submission2_a4 / stacking вне train | **349.31**, задокументировано |
| `final_submission.csv` | Baseline ~365 | Генерируется `--predict`; не равен 349 без внешнего stacking |

**Вывод:** презентация **корректно разделяет** «ядро воспроизводимости в git» (~365) и «лучший LB команды» (349, offline stacking). Противоречия нет.

---

## 4. Оценка по критериям хакатона (100 баллов)

Модель оценки хакатона (100 баллов): Kaggle до 85 + код 5 + обоснование 5 + оригинальность 5 + защита 15 (топ-10).

| Критерий | Макс | Оценка | Обоснование |
|----------|-----:|-------:|-------------|
| **Kaggle (ранг ~90, score 349)** | 85 | **~35–42** | Пропорционально рангу; не топ-20 |
| **Качество кода** | 5 | **4–5** | Структура `chemai`, ruff, 9 тестов, README, Makefile |
| **Обоснование** | 5 | **4–4.5** | EDA, итоговая записка, архитектурная записка, презентация |
| **Оригинальность** | 5 | **2.5–3** | Стандартный QSPR-стек; ClusterKFold + stacking offline |
| **Защита (топ-10)** | 15 | **0** | Ранг ~90 |

**Суммарная оценка (без защиты): ~46–54 / 100** — согласуется с планом «ранг ~90».

При наличии офлайн-эксперимента **345** (не в git) в устной защите можно упомянуть потенциал, но **не заявлять как официальный результат платформы**.

---

## 5. Качество кода и тесты

| Проверка | Результат |
|----------|-----------|
| `pytest -q` | **9 passed** (состояние `4a1bfa0`) |
| `ruff check` | Настроен в pyproject |
| Один entrypoint | `run_pipeline.py` |
| Нет stacking/phase0 в коде | ✅ откат |

**Сохранено из улучшений покрытия:** `test_metrics.py`, `test_ensemble.py`, ужесточённый `test_candidate_registry.py`.

**Откатано (в бэклоге):** `test_phase0.py`, `test_phase1_cv.py` (14 тестов Фазы 1).

---

## 6. ИБ

См. [`ИБ_ревью_2026-06-22_финальная_сдача.md`](ИБ_ревью_2026-06-22_финальная_сдача.md). **Блокеров нет.**

---

## 7. Чеклист сдачи на платформу

| # | Пункт | Готово |
|---|-------|--------|
| 1 | Ссылка на Git-репозиторий | ✅ |
| 2 | Код решения | ✅ |
| 3 | README + инструкция запуска | ✅ |
| 4 | Презентация в репо | ✅ PDF + draft |
| 5 | Описание подхода (EDA, записки) | ✅ `docs/eda/` |
| 6 | Код ≡ презентация | ✅ после отката |
| 7 | Видео 5–7 мин | ⚪ только топ-10 |

---

## 8. Рекомендуемые улучшения (не блокируют сдачу)

1. **Cherry-pick Epic 10** (clip + tree-only) + тесты — для портфолио, не для текущей сдачи.
2. **Ablation-таблица** в README (1 строка на эксперимент) — +0.5 к «обоснованию».
3. **CI workflow** (pytest + ruff) — +0.5 к «качеству кода».
4. **Mini-dataset** в git или явный скрипт загрузки Kaggle — закрыть бэклог §1.

---

## 9. Подтверждение готовности

**Проект готов к сдаче** в текущем состоянии:

- Код **соответствует презентации** и ТЗ.
- Лучший **официальный** результат Kaggle (**349.31**) **документирован** и не противоречит архитектуре git.
- Наработки 22.06 (**345**, фазы 0–1, эпики 9–11) **перенесены в бэклог**, код откатан.
- ИБ-риски для офлайн-пайплайна **приемлемы**.

**Действия перед загрузкой ссылки:** убедиться, что ветка на remote синхронизирована с `4a1bfa0` (при необходимости force-push согласовать с командой — локально выполнен `git reset --hard 4a1bfa0`).
