# Оценка готовности проекта к сдаче

**Дата обновления:** 2026-06-23  
**Состояние репозитория:** `aff1492` — код презентации + документация сдачи + опциональные domain-улучшения  
**Ветка:** `docs/author1-final-analytical-memo` (синхронизирована с origin)  
**Kaggle:** новые сабмиты **невозможны**; лучший командный public LB **349.31** (ранг ~90)

---

## 1. Соответствие ТЗ (`Разработка/подготовка/ТЗ_Хакатон.txt`)

| Требование ТЗ | Статус | Где в репо |
|---------------|--------|------------|
| Предсказание IC50, CC50, SI | ✅ | `train.py`, `predict.py`, `TARGETS` |
| Pipeline решения | ✅ | `run_pipeline.py --train` / `--predict` |
| CSV: `index, IC50, CC50, SI` | ✅ | `predict.py`, `postprocess.py` |
| Метрика: mean(RMSE×3) | ✅ | `utils/metrics.py` → `competition_score` |
| Код в Git | ✅ | `ml/src/chemai/` |
| README + воспроизведение | ✅ | `README.md`, `.env.example`, `Makefile` |
| Презентация | ✅ | `docs/presentation/39_Презентация_ChemAI-Predict-the-Cure.pdf` |
| Видео (топ-10) | N/A | Команда не в топ-10 |

**Замечания (некритичные):**

- Данные `train.csv`/`test.csv` **не в git** (`.gitignore`) — допустимо при инструкции в README.
- SI прогнозируется **отдельной моделью**; опциональная post-hoc смесь с CC50/IC50 (`CHEM_SI_DOMAIN_BLEND`, по умолчанию `0`) **не меняет** baseline.

**Вердикт по ТЗ:** выполнено, блокеров нет.

---

## 2. Презентация vs код (актуальность)

| Утверждение презентации | Код `aff1492` | Совпадение |
|-------------------------|---------------|------------|
| ClusterKFold | `validation/cv_splitter.py` | ✅ |
| Per-fold Preprocessor | `train.py` — fit только на train фолда | ✅ |
| 9 семейств + опц. CatBoost | `build_default_candidates` | ✅ |
| Взвешенный ансамбль, **без stacking** в `train.py` | `Ensemble`, веса из CV RMSE | ✅ |
| log1p IC50/CC50 + expm1 | `Expm1Predictor`, config flag | ✅ |
| Лучший LB **~349** (submission2_a4) | Документировано в `docs/eda/` | ✅ |
| Baseline repo ~365 | `final_submission.csv` после `--predict` | ✅ |
| tools/ удалён | Нет каталога `tools/` | ✅ |

**Добавлено в `aff1492` (не противоречит PDF, default = старое поведение):**

- признак `FractionCSP3_LogP` в `build_features.py`;
- `CHEM_SI_DOMAIN_BLEND=0.0` — опциональная доменная смесь SI;
- `docs/eda/Ablation_экспериментов_оригинальность.md`, раздел в README.

### Требуется ли обновление презентации?

**Нет — для сдачи.** PDF согласован с ядром пайплайна.

**Опционально:** слайд про ablation / 11 pytest; офлайн **345.08** — только в бэклоге, не на слайдах.

---

## 3. Соответствие обученным моделям

| Артефакт | Ожидание | Факт |
|----------|----------|------|
| `pipeline_bundle.joblib` | После `--train` | Локальный; **не в git** |
| Лучший Kaggle | submission2_a4 | **349.31**, задокументировано |
| `final_submission.csv` | Baseline ~365 | Генерируется `--predict` |

**Вывод:** презентация корректно разделяет «ядро git» (~365) и «лучший LB» (349, stacking вне `train.py`).

---

## 4. Оценка по критериям хакатона (100 баллов)

Модель: Kaggle до 85 + код 5 + обоснование 5 + оригинальность 5 + защита 15 (топ-10).

| Критерий | Макс | Оценка | Обоснование |
|----------|-----:|-------:|-------------|
| **Kaggle (ранг ~90, score 349)** | 85 | **~35–42** | Пропорционально рангу |
| **Качество кода** | 5 | **4.5–5** | `chemai`, ruff, **11** тестов, README, Makefile, ИБ-ревью |
| **Обоснование** | 5 | **4.5–5** | EDA, записки, ablation, бэклог экспериментов |
| **Оригинальность** | 5 | **3–3.5** | ClusterKFold, domain features, опц. SI blend, offline stacking |
| **Защита (топ-10)** | 15 | **0** | Ранг ~90 |

**Суммарная оценка (без защиты): ~48–56 / 100.**

---

## 5. Качество кода и тесты

| Проверка | Результат (`aff1492`) |
|----------|------------------------|
| `pytest -q` | **11 passed** |
| `ruff check` | Настроен в pyproject |
| Один entrypoint | `run_pipeline.py` |
| Нет stacking/phase0 в коде | ✅ |

Дополнительно: `test_postprocess.py` (SI blend), `test_features.py` (FractionCSP3_LogP).

---

## 6. ИБ

См. [`ИБ_ревью_2026-06-22_финальная_сдача.md`](ИБ_ревью_2026-06-22_финальная_сдача.md). **Блокеров нет.**

---

## 7. Чеклист сдачи на платформу

| # | Пункт | Готово |
|---|-------|--------|
| 1 | Ссылка на Git-репозиторий | ✅ |
| 2 | Код решения | ✅ |
| 3 | README + инструкция запуска | ✅ |
| 4 | Презентация в репо | ✅ PDF + draft |
| 5 | Описание подхода (EDA, записки, ablation) | ✅ |
| 6 | Код ≡ презентация | ✅ |
| 7 | Видео 5–7 мин | ⚪ только топ-10 |

---

## 8. Рекомендуемые улучшения (не блокируют сдачу)

1. CI workflow (pytest + ruff) — усилит «качество кода».
2. Mini-dataset в git или скрипт загрузки Kaggle — бэклог §1.
3. Cherry-pick Epic 10 (clip + tree-only) — только для портфолио; не для текущей сдачи.

---

## 9. Подтверждение готовности

**Проект готов к сдаче** на `aff1492`:

- Код соответствует **презентации** и **ТЗ**.
- Лучший официальный Kaggle (**349.31**) документирован.
- Наработки 22.06 перенесены в [`Бэклог/Наработки_2026-06-22_после_закрытия_Kaggle.md`](../Бэклог/Наработки_2026-06-22_после_закрытия_Kaggle.md).
- Remote синхронизирован (`git push --force-with-lease` выполнен).

**Перед сдачей:** указать на платформе ссылку на репозиторий; не заявлять, что `train.py` воспроизводит LB 349 без оговорки про submission2_a4.
