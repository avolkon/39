# Задание на разработку: Эпик 1 — Инфраструктура и конфигурация

## Контекст
Без общего каркаса нельзя воспроизвести обучение и сдачу submission на разных машинах.

## Текущее состояние
**Файлы:** `pyproject.toml`, `.env.example`, `Makefile`, `run_pipeline.py`, `src/chemai/utils/config.py`

## Требования ТЗ
Зависимости зафиксированы; пути и seed читаются из окружения; одна точка входа CLI.

## Задача
Реализовать установку пакета, конфиг `CHEM_*`, команды `make lint/test/train/predict`.

## Технические требования
1. `load_config` / `get_config` с Pydantic Settings.
2. Логирование через `logging`, не `print`.

## Критерии приемки
- [x] Editable install работает
- [x] `ruff check` без ошибок
- [x] `pytest` проходит

**Приоритет:** Высокий  
**Зависимости:** нет
